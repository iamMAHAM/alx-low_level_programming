#include "main.h"

/**
 * _islower - check if character is lowercase
 * @charac: the charactere to check
 * Return: 1 if charac is lowerse else 0
 */
int _islower(int charac)
{
	return (c => 'a' && c <= 'z');
}
